
\newpage

.. Generated Save Procedure

~sp~
DELIMITER //
CREATE PROCEDURE spgen_employees_save
(
    $ORG_ID              INT,
    $EMPLOYEE_ID         INT,
    $name                TEXT,
    $value               TEXT
)
SQL SECURITY INVOKER
COMMENT 'Generated by spgen.org'
BEGIN

    IF @@read_only THEN

        SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'READ_ONLY';

    ELSE

        CASE $name
        WHEN 'employee_first_name' THEN UPDATE employees SET employee_first_name = $value WHERE employee_deleted = 0 AND ORG_ID = $ORG_ID AND EMPLOYEE_ID = $EMPLOYEE_ID;
        WHEN 'employee_last_type' THEN UPDATE employees SET employee_last_type = $value WHERE employee_deleted = 0 AND ORG_ID = $ORG_ID AND EMPLOYEE_ID = $EMPLOYEE_ID;
        ELSE
    
            SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'INVALID CASE OPTION IN employees_save';
    
        END CASE;

    END IF;

END
//
DELIMITER ;
~
